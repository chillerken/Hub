const crypto = require('node:crypto');
const { verifyStripe } = require('./core/webhooks');

const CHECKOUT = {
  starter_monthly: process.env.LR_CHECKOUT_STARTER_MONTHLY || 'https://buy.stripe.com/test_7sY9AU1zw5Q57Sp1xvdfG03',
  growth_monthly: process.env.LR_CHECKOUT_GROWTH_MONTHLY || 'https://buy.stripe.com/test_6oUcN6cea5Q5fkR5NLdfG04',
  starter_annual: process.env.LR_CHECKOUT_STARTER_ANNUAL || 'https://buy.stripe.com/test_eVq4gAcea6U9egN7VTdfG05',
  growth_annual: process.env.LR_CHECKOUT_GROWTH_ANNUAL || 'https://buy.stripe.com/test_3cI5kE2DA1zPegN0trdfG06'
};

const PLANS = {
  starter: { name: 'Starter', monthly: 79, annual: 790, leadLimit: 100, followups: 2 },
  growth: { name: 'Growth', monthly: 149, annual: 1490, leadLimit: 500, followups: 4 }
};

const css = `
:root{--bg:#07111f;--card:#0d1b2e;--line:#20324d;--text:#eef4ff;--muted:#9db0c9;--accent:#67e8f9;--green:#86efac;--warn:#fde68a;--danger:#fda4af;--max:1120px}
*{box-sizing:border-box}body{margin:0;background:linear-gradient(180deg,#06101d,#0b1422 55%,#07111f);color:var(--text);font:16px/1.55 Inter,system-ui,-apple-system,Segoe UI,sans-serif}a{color:inherit}nav{border-bottom:1px solid var(--line);background:#07111fe8;position:sticky;top:0;z-index:4}.nav{max-width:var(--max);margin:auto;padding:14px 20px;display:flex;align-items:center;gap:18px}.brand{font-weight:900;letter-spacing:-.03em;font-size:21px}.brand i{font-style:normal;color:var(--accent)}.navlinks{margin-left:auto;display:flex;gap:15px;align-items:center;flex-wrap:wrap}.wrap{max-width:var(--max);margin:auto;padding:46px 20px}.hero{padding:66px 0 44px;display:grid;grid-template-columns:1.2fr .8fr;gap:36px;align-items:center}.eyebrow{color:var(--accent);font-weight:800;text-transform:uppercase;letter-spacing:.12em;font-size:12px}.hero h1{font-size:clamp(42px,7vw,76px);line-height:.98;margin:12px 0 18px;letter-spacing:-.055em}.hero p,.muted{color:var(--muted)}.hero p{font-size:20px;max-width:720px}.actions{display:flex;gap:12px;flex-wrap:wrap;margin-top:26px}.btn{display:inline-block;background:var(--accent);color:#04202a;text-decoration:none;border:0;border-radius:12px;padding:12px 18px;font-weight:900;cursor:pointer}.btn.secondary{background:transparent;color:var(--text);border:1px solid var(--line)}.btn.small{padding:8px 12px;font-size:14px}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}.card{background:linear-gradient(180deg,#0e1d31,#0b1728);border:1px solid var(--line);border-radius:18px;padding:22px;box-shadow:0 18px 55px #0003}.card h2,.card h3{margin-top:0}.metric{font-size:34px;font-weight:900}.badge{display:inline-flex;padding:5px 9px;border-radius:999px;background:#12334a;color:var(--accent);font-size:12px;font-weight:800}.badge.ok{background:#123520;color:var(--green)}.badge.warn{background:#443614;color:var(--warn)}.section{padding:28px 0}.section h2{font-size:36px;letter-spacing:-.04em}.price{font-size:42px;font-weight:900}.price small{font-size:14px;color:var(--muted)}ul.clean{padding-left:20px;color:var(--muted)}.banner{border:1px solid #6a5416;background:#2b240f;color:var(--warn);border-radius:12px;padding:12px 15px;margin:18px 0}.form{max-width:720px}.form label{display:block;font-weight:800;margin:15px 0 6px}.form input,.form textarea,.form select{width:100%;border:1px solid var(--line);background:#091729;color:var(--text);border-radius:10px;padding:12px;font:inherit}.form textarea{min-height:115px}.row{display:grid;grid-template-columns:1fr 1fr;gap:14px}.tablewrap{overflow:auto;border:1px solid var(--line);border-radius:14px}.table{width:100%;border-collapse:collapse;min-width:720px}.table th,.table td{padding:12px;border-bottom:1px solid var(--line);text-align:left}.table th{font-size:12px;text-transform:uppercase;letter-spacing:.08em;color:var(--muted)}.code{background:#030a12;border:1px solid var(--line);border-radius:12px;padding:14px;overflow:auto;color:#b7f7ff;white-space:pre-wrap;word-break:break-all}.kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:14px}.footer{border-top:1px solid var(--line);margin-top:60px}.footer .wrap{padding-top:26px;padding-bottom:26px;color:var(--muted)}.leadbox{border:1px solid var(--line);border-radius:18px;padding:22px;background:#0c192a}.notice{padding:12px 14px;border-radius:10px;background:#10263a;color:#cceeff;margin-bottom:16px}.danger{color:var(--danger)}
@media(max-width:820px){.hero{grid-template-columns:1fr}.grid,.kpis{grid-template-columns:1fr 1fr}.navlinks a.hide-mobile{display:none}.row{grid-template-columns:1fr}}@media(max-width:520px){.grid,.kpis{grid-template-columns:1fr}.wrap{padding-left:15px;padding-right:15px}.hero h1{font-size:48px}.nav{padding:12px 15px}}
`;

const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const emailOk = value => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(String(value || ''));
const parseCookie = header => Object.fromEntries(String(header || '').split(';').map(v=>v.trim()).filter(Boolean).map(v=>{const i=v.indexOf('=');return [decodeURIComponent(i<0?v:v.slice(0,i)),decodeURIComponent(i<0?'':v.slice(i+1))]}));
const b64 = value => Buffer.from(JSON.stringify(value)).toString('base64url');
const unb64 = value => JSON.parse(Buffer.from(value,'base64url').toString('utf8'));
const safe = (a,b) => { const A=Buffer.from(String(a)), B=Buffer.from(String(b)); return A.length===B.length && crypto.timingSafeEqual(A,B); };

function sessionToken(secret, accountId, now=Date.now()) {
  const payload=b64({sub:accountId,exp:Math.floor(now/1000)+60*60*24*14});
  const sig=crypto.createHmac('sha256',`${secret}:replyloop-session`).update(payload).digest('base64url');
  return `${payload}.${sig}`;
}
function verifySession(secret, token, now=Date.now()) {
  try{const [payload,sig]=String(token||'').split('.');if(!payload||!sig)return null;const expected=crypto.createHmac('sha256',`${secret}:replyloop-session`).update(payload).digest('base64url');if(!safe(sig,expected))return null;const p=unb64(payload);if(!p.sub||p.exp<Math.floor(now/1000))return null;return p;}catch{return null;}
}
function passwordRecord(password, salt=crypto.randomBytes(16).toString('hex')) {
  const hash=crypto.scryptSync(String(password),salt,64).toString('hex'); return {salt,hash};
}
function passwordValid(password, record) {
  try{return safe(crypto.scryptSync(String(password),record.password_salt,64).toString('hex'),record.password_hash);}catch{return false;}
}
function publicKey(secret, accountId){const sig=crypto.createHmac('sha256',`${secret}:replyloop-public`).update(accountId).digest('base64url').slice(0,22);return `${accountId}.${sig}`;}
function publicKeyId(secret,key){const [id,sig]=String(key||'').split('.');if(!/^[a-f0-9-]{36}$/.test(id||''))return null;const expected=publicKey(secret,id).split('.')[1];return safe(sig,expected)?id:null;}

function layout(title, body, {authed=false,billingMode='sandbox'}={}) {
  return `<!doctype html><html lang="nl-BE"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="ReplyLoop volgt nieuwe websiteleads automatisch op voor lokale dienstverleners."><title>${esc(title)} · ReplyLoop</title><link rel="stylesheet" href="/replyloop.css"></head><body><nav><div class="nav"><a class="brand" href="/replyloop">Reply<i>Loop</i></a><div class="navlinks"><a class="hide-mobile" href="/replyloop/calculator">Gratis calculator</a><a class="hide-mobile" href="/replyloop/pricing">Prijzen</a>${authed?'<a href="/replyloop/app">Dashboard</a><form method="post" action="/api/replyloop/logout"><button class="btn secondary small" type="submit">Uitloggen</button></form>':'<a href="/replyloop/login">Inloggen</a><a class="btn small" href="/replyloop/signup">Start</a>'}</div></div></nav><main><div class="wrap">${billingMode==='sandbox'?'<div class="banner"><strong>Stripe sandbox actief.</strong> De volledige betaalflow kan worden getest, maar er wordt nog geen echt geld geïnd. Live Stripe moet nog éénmalig worden geactiveerd.</div>':''}${body}</div></main><footer class="footer"><div class="wrap">ReplyLoop · snelle leadopvolging voor lokale dienstverleners · <a href="/replyloop/privacy">Privacy</a> · <a href="/replyloop/terms">Voorwaarden</a></div></footer></body></html>`;
}

function home(mode){return layout('Automatische leadopvolging',`<section class="hero"><div><div class="eyebrow">Van aanvraag naar afspraak, automatisch</div><h1>Laat geen warme lead koud worden.</h1><p>ReplyLoop reageert direct op websiteaanvragen, volgt automatisch op en toont welke leads nog aandacht nodig hebben. Voor lokale dienstverleners die geen tijd willen verliezen aan manuele opvolging.</p><div class="actions"><a class="btn" href="/replyloop/signup?plan=starter&billing=monthly">Start voor €79/maand</a><a class="btn secondary" href="/replyloop/calculator">Bereken gemiste omzet</a></div></div><div class="card"><span class="badge ok">Automatische flow</span><h2>Nieuwe aanvraag</h2><p class="muted">0 min → bevestiging<br>24 u → follow-up<br>72 u → laatste opvolging<br>Daarna alleen nog uitzonderingen.</p><div class="metric">Setup once.</div><div class="muted">Reply → Follow-up → Book → Review → Referral</div></div></section><section class="section"><h2>Wat het overneemt</h2><div class="grid"><div class="card"><h3>⚡ Direct antwoord</h3><p class="muted">Een nieuwe webaanvraag krijgt automatisch een professionele bevestiging namens het bedrijf.</p></div><div class="card"><h3>↻ Follow-up</h3><p class="muted">Geen antwoord? ReplyLoop stuurt geplande opvolging zonder dat iemand een lijst moet bijhouden.</p></div><div class="card"><h3>◎ Alleen uitzonderingen</h3><p class="muted">Het dashboard toont warme, onbeantwoorde en vastgelopen leads zodat de eigenaar alleen ingrijpt waar het telt.</p></div></div></section><section class="section"><h2>Eenvoudige prijs, meetbare waarde</h2><div class="grid"><div class="card"><span class="badge">Starter</span><div class="price">€79 <small>/ maand</small></div><ul class="clean"><li>100 leads per maand</li><li>2 automatische follow-ups</li><li>Websiteformulier/API</li><li>Lead-dashboard</li><li>Referral-link</li></ul><a class="btn" href="/replyloop/signup?plan=starter&billing=monthly">Kies Starter</a></div><div class="card"><span class="badge ok">Growth</span><div class="price">€149 <small>/ maand</small></div><ul class="clean"><li>500 leads per maand</li><li>4 automatische follow-ups</li><li>Review- en referralflow</li><li>Uitgebreide statistieken</li><li>Prioritaire support</li></ul><a class="btn" href="/replyloop/signup?plan=growth&billing=monthly">Kies Growth</a></div><div class="card"><span class="badge">Jaarlijks</span><div class="price">2 maanden <small>bespaard</small></div><p class="muted">Starter €790/jaar. Growth €1.490/jaar. Zelfde product, minder churn en voorspelbare kosten.</p><a class="btn secondary" href="/replyloop/pricing">Alle prijzen</a></div></div></section>`,{billingMode:mode});}

function pricing(mode){return layout('Prijzen',`<div class="eyebrow">Pricing</div><h1>Betaal voor opvolging, niet voor complexiteit.</h1><p class="muted">Geen setup-fee. Maandelijks opzegbaar. Jaarlijks is gelijk aan ongeveer twee maanden voordeel.</p><div class="grid section"><div class="card"><h2>Starter</h2><div class="price">€79 <small>/maand</small></div><p class="muted">of €790/jaar</p><ul class="clean"><li>100 inkomende leads/maand</li><li>2 follow-ups per lead</li><li>Dashboard en pipeline</li><li>Automatische ontvangstmail</li></ul><div class="actions"><a class="btn" href="/replyloop/signup?plan=starter&billing=monthly">Maandelijks</a><a class="btn secondary" href="/replyloop/signup?plan=starter&billing=annual">Jaarlijks</a></div></div><div class="card"><h2>Growth</h2><div class="price">€149 <small>/maand</small></div><p class="muted">of €1.490/jaar</p><ul class="clean"><li>500 inkomende leads/maand</li><li>4 follow-ups per lead</li><li>Review + referral</li><li>Prioritaire opvolging</li></ul><div class="actions"><a class="btn" href="/replyloop/signup?plan=growth&billing=monthly">Maandelijks</a><a class="btn secondary" href="/replyloop/signup?plan=growth&billing=annual">Jaarlijks</a></div></div><div class="card"><h2>MRR-doel</h2><p class="muted">Bij €79 zijn 13 Starter-klanten ≈ €1.027 MRR. Bij €149 zijn 7 Growth-klanten ≈ €1.043 MRR. Een mix van beide verlaagt de supportdruk en verhoogt de gemiddelde opbrengst per klant.</p></div></div>`,{billingMode:mode});}

function signupPage(plan='starter',billing='monthly',ref='',mode='sandbox',error=''){plan=PLANS[plan]?plan:'starter';billing=['monthly','annual'].includes(billing)?billing:'monthly';const p=PLANS[plan],price=billing==='annual'?p.annual:p.monthly;return layout('Account maken',`<div class="form"><div class="eyebrow">${esc(p.name)} · ${billing==='annual'?'jaarlijks':'maandelijks'}</div><h1>Maak je account.</h1><p class="muted">Na registratie ga je naar Stripe. Je account wordt automatisch actief zodra de betaling bevestigd is.</p>${error?`<div class="notice danger">${esc(error)}</div>`:''}<form method="post" action="/api/replyloop/signup"><input type="hidden" name="plan" value="${esc(plan)}"><input type="hidden" name="billing" value="${esc(billing)}"><input type="hidden" name="ref" value="${esc(ref)}"><label>Bedrijfsnaam</label><input name="business_name" maxlength="100" required><label>Jouw naam</label><input name="name" maxlength="100" required><label>E-mail</label><input type="email" name="email" maxlength="160" required><label>Wachtwoord</label><input type="password" name="password" minlength="10" maxlength="200" required><label><input type="checkbox" name="terms" value="yes" required> Ik ga akkoord met de voorwaarden en privacyverklaring.</label><div class="actions"><button class="btn" type="submit">Verder naar Stripe · €${price}${billing==='monthly'?'/m':'/jaar'}</button></div></form></div>`,{billingMode:mode});}

function loginPage(mode,error=''){return layout('Inloggen',`<div class="form"><div class="eyebrow">Klantportaal</div><h1>Inloggen</h1>${error?`<div class="notice danger">${esc(error)}</div>`:''}<form method="post" action="/api/replyloop/login"><label>E-mail</label><input type="email" name="email" required><label>Wachtwoord</label><input type="password" name="password" required><div class="actions"><button class="btn" type="submit">Inloggen</button></div></form></div>`,{billingMode:mode});}

function calculatorPage(mode,result=null){let resultHtml='';if(result){resultHtml=`<div class="card"><div class="eyebrow">Schatting</div><div class="metric">€${esc(result.monthlyLost)}</div><p class="muted">potentiële gemiste omzet per maand bij ${esc(result.missed)} gemiste/te traag opgevolgde leads.</p><p>Zelfs als slechts 25% daarvan herstelbaar is: <strong>€${esc(result.recoverable)}/maand</strong>.</p><a class="btn" href="/replyloop/signup">Automatiseer opvolging</a></div>`;}return layout('Gemiste lead calculator',`<div class="hero"><div><div class="eyebrow">Gratis tool</div><h1>Wat kost trage leadopvolging?</h1><p>Vul drie cijfers in. Geen account nodig.</p><form class="form" method="post" action="/api/replyloop/calculator"><label>Nieuwe leads per maand</label><input type="number" name="leads" min="1" max="100000" value="40" required><label>Gemiddelde waarde van één klant (€)</label><input type="number" name="value" min="1" max="1000000" value="250" required><label>% leads dat afhaakt door trage of ontbrekende opvolging</label><input type="number" name="loss" min="1" max="100" value="20" required><div class="actions"><button class="btn" type="submit">Bereken</button></div></form></div>${resultHtml||'<div class="card"><h2>Waarom dit telt</h2><p class="muted">Een lead die al contact opneemt heeft koopintentie. ReplyLoop focust daarom niet op meer verkeer, maar op sneller en consistenter reageren op het verkeer dat er al is.</p></div>'}</div>`,{billingMode:mode});}

function nichePage(mode,niche,label){return layout(`Leadopvolging voor ${label}`,`<div class="hero"><div><div class="eyebrow">ReplyLoop voor ${esc(label)}</div><h1>Meer uit elke ${esc(niche)}-aanvraag.</h1><p>Automatische bevestiging en opvolging voor ${esc(label)} die vaak onderweg, op locatie of met klanten bezig zijn. Nieuwe websiteleads verdwijnen niet meer tussen mailbox, telefoon en agenda.</p><div class="actions"><a class="btn" href="/replyloop/signup">Start ReplyLoop</a><a class="btn secondary" href="/replyloop/calculator">Bereken gemiste omzet</a></div></div><div class="card"><h2>Flow</h2><p class="muted">Websiteaanvraag → directe reactie → gerichte opvolging → status in dashboard → review/referral na gewonnen lead.</p></div></div>`,{billingMode:mode});}

function legalPage(mode,type){const privacy=type==='privacy';return layout(privacy?'Privacy':'Voorwaarden',`<div class="form"><h1>${privacy?'Privacybeleid':'Gebruiksvoorwaarden'}</h1>${privacy?'<p>ReplyLoop verwerkt accountgegevens en leadgegevens uitsluitend om de dienst te leveren. Klanten blijven verantwoordelijk voor hun eigen wettelijke grondslag, transparantie richting hun leads en het respecteren van opt-outs. Gegevens worden niet verkocht. Voor productiegebruik worden alleen noodzakelijke gegevens bewaard en toegang wordt technisch beperkt.</p><p>Voor marketingmails is afzonderlijke toestemming of een andere geldige wettelijke grondslag nodig. Operationele reactie op een door de lead zelf ingediende aanvraag wordt gescheiden van marketing.</p>':'<p>ReplyLoop is software voor automatische opvolging van inkomende leads. De klant gebruikt de dienst rechtmatig, stuurt geen ongevraagde massacommunicatie en respecteert uitschrijvingen. Abonnementen lopen tot opzegging volgens de gekozen facturatieperiode. Resultaten zijn afhankelijk van aanbod, markt, responstijd en leadkwaliteit; omzet wordt niet gegarandeerd.</p><p>De klant is verantwoordelijk voor correcte bedrijfsinformatie, verzendteksten en gekoppelde kanalen. Bij misbruik kan toegang worden beperkt.</p>'}<p class="muted">Versie 15 september 2026. Definitieve juridische review wordt aangeraden vóór brede internationale verkoop.</p></div>`,{billingMode:mode});}

async function rawBody(req){let data='';for await(const c of req){data+=c;if(Buffer.byteLength(data)>100000)throw Object.assign(new Error('Aanvraag te groot'),{status:413});}return data;}
function parseBody(raw,ct=''){if(ct.includes('application/json'))return raw?JSON.parse(raw):{};const q=new URLSearchParams(raw);return Object.fromEntries(q.entries());}
function noteJson(row){try{const v=JSON.parse(row.content);return v&&typeof v==='object'?v:null;}catch{return null;}}

module.exports = function makeReplyLoop(config, store, deps={}) {
  const db=deps.db || require('./core/db')(config);
  const doFetch=deps.fetch || fetch;
  const billingMode=process.env.LR_BILLING_MODE==='live'?'live':'sandbox';

  async function exportAccount(id){return await db('export_customer',{id});}
  async function accountById(id){const ex=await exportAccount(id);const rows=(ex?.customer_notes||[]).map(r=>({row:r,data:noteJson(r)})).filter(x=>x.data?.lr_type==='account').sort((a,b)=>new Date(b.row.created_at)-new Date(a.row.created_at));return rows[0]?{customer:ex.customer,note:rows[0].row,data:rows[0].data,export:ex}:null;}
  async function accountByEmail(email){const matches=await db('customer_find',{email:String(email).toLowerCase(),phone:''});for(const c of matches||[]){const a=await accountById(c.id);if(a&&String(a.data.email).toLowerCase()===String(email).toLowerCase())return a;}return null;}
  async function saveAccount(id,data){await db('save',{table:'customer_notes',data:{customer_id:id,content:JSON.stringify({...data,updated_at:new Date().toISOString()})}});return accountById(id);}
  async function sessionAccount(req){const token=parseCookie(req.headers.cookie).replyloop_session;const claims=verifySession(config.cookieSecret,token);return claims?accountById(claims.sub).catch(()=>null):null;}
  function setSession(id){const token=sessionToken(config.cookieSecret,id);return `replyloop_session=${encodeURIComponent(token)}; HttpOnly; SameSite=Lax; Secure; Path=/; Max-Age=1209600`;}
  function clearSession(){return 'replyloop_session=; HttpOnly; SameSite=Lax; Secure; Path=/; Max-Age=0';}
  function checkoutUrl(data,id){const key=`${data.plan}_${data.billing}`;const base=CHECKOUT[key]||CHECKOUT.starter_monthly;return `${base}?locked_prefilled_email=${encodeURIComponent(data.email)}&client_reference_id=${encodeURIComponent(id)}&locale=nl`;}
  async function sendMail({to,subject,text,replyTo}){if(!config.resend.apiKey||!config.resend.from||!emailOk(to))return {ok:false,reason:'email_not_configured'};const payload={from:config.resend.from,to:[to],subject,text};if(replyTo&&emailOk(replyTo))payload.reply_to=replyTo;const r=await doFetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${config.resend.apiKey}`,'Content-Type':'application/json'},body:JSON.stringify(payload),signal:AbortSignal.timeout(15000)});const data=await r.json().catch(()=>({}));return {ok:r.ok,id:data.id||null,error:r.ok?null:(data.message||`HTTP ${r.status}`)};}
  function replyLoopAccountId(lead){const meta=lead&&lead.metadata&&typeof lead.metadata==='object'?lead.metadata:{};const tagged=String(meta.replyloop_account_id||'');if(/^[a-f0-9-]{36}$/i.test(tagged))return tagged;const source=String(lead?.source||'');return source.startsWith('replyloop:')?source.slice('replyloop:'.length):'';}
  function isReplyLoopLead(lead){return Boolean(replyLoopAccountId(lead));}
  function leadsFor(accountId,all){return (all||[]).filter(l=>replyLoopAccountId(l)===accountId).sort((a,b)=>new Date(b.created_at)-new Date(a.created_at));}

  async function dashboard(a){const all=await store.listLeads();const leads=leadsFor(a.customer.id,all);const now=Date.now();const won=leads.filter(x=>x.status==='won').length;const open=leads.filter(x=>!['won','lost'].includes(x.status)).length;const thisMonth=leads.filter(x=>{const d=new Date(x.created_at),n=new Date();return d.getUTCFullYear()===n.getUTCFullYear()&&d.getUTCMonth()===n.getUTCMonth();}).length;const active=a.data.status==='active';const key=publicKey(config.cookieSecret,a.customer.id);const snippet=`<form action="${config.baseUrl}/api/replyloop/leads?key=${key}" method="post">\n  <input name="name" placeholder="Naam" required>\n  <input type="email" name="email" placeholder="E-mail" required>\n  <input name="phone" placeholder="Telefoon">\n  <input name="service" placeholder="Waarmee kunnen we helpen?">\n  <textarea name="message" placeholder="Vraag"></textarea>\n  <button type="submit">Verstuur</button>\n</form>`;const rows=leads.slice(0,50).map(l=>`<tr><td>${esc(l.name)}</td><td>${esc(l.service||'—')}</td><td>${esc(l.status)}</td><td>${esc(new Date(l.created_at).toLocaleDateString('nl-BE'))}</td><td>${esc(l.next_action||'—')}</td></tr>`).join('')||'<tr><td colspan="5">Nog geen leads.</td></tr>';return layout('Dashboard',`<div class="eyebrow">${esc(a.data.business_name)}</div><h1>Lead dashboard</h1>${active?'<div class="notice">Automatisering actief.</div>':`<div class="banner">Abonnementstatus: <strong>${esc(a.data.status)}</strong>. Activeer Stripe om automatische verwerking volledig in te schakelen. <a href="${esc(checkoutUrl(a.data,a.customer.id))}">Naar checkout</a></div>`}<div class="kpis"><div class="card"><div class="muted">Leads deze maand</div><div class="metric">${thisMonth}</div></div><div class="card"><div class="muted">Open</div><div class="metric">${open}</div></div><div class="card"><div class="muted">Gewonnen</div><div class="metric">${won}</div></div><div class="card"><div class="muted">Plan</div><div class="metric">${esc(PLANS[a.data.plan]?.name||'Starter')}</div></div></div><section class="section"><div class="grid"><div class="card"><h2>Onboarding</h2><p class="muted">Diensten, tone of voice, boekingslink en gemiddelde klantwaarde.</p><a class="btn secondary" href="/replyloop/onboarding">Instellingen aanpassen</a></div><div class="card"><h2>Referral</h2><p class="muted">Deel je persoonlijke link. Referrals worden geregistreerd bij inschrijving.</p><div class="code">${esc(`${config.baseUrl}/replyloop/signup?ref=${a.customer.id}`)}</div></div><div class="card"><h2>Gebruik</h2><p class="muted">${thisMonth} / ${PLANS[a.data.plan]?.leadLimit||100} leads deze maand.</p><p>${Math.max(0,(PLANS[a.data.plan]?.leadLimit||100)-thisMonth)} resterend.</p></div></div></section><section class="section"><h2>Websiteformulier koppelen</h2><p class="muted">Plaats dit formulier op een site of stuur POST-data rechtstreeks naar hetzelfde endpoint. Geen API-key in JavaScript nodig: de publieke sleutel is ondertekend en geeft alleen toestemming om een lead toe te voegen.</p><div class="code">${esc(snippet)}</div></section><section class="section"><h2>Recente leads</h2><div class="tablewrap"><table class="table"><thead><tr><th>Naam</th><th>Dienst</th><th>Status</th><th>Datum</th><th>Volgende actie</th></tr></thead><tbody>${rows}</tbody></table></div></section>`,{authed:true,billingMode});}

  async function onboardingPage(a,msg=''){return layout('Onboarding',`<div class="form"><div class="eyebrow">Eenmalige setup</div><h1>Bedrijfscontext</h1>${msg?`<div class="notice">${esc(msg)}</div>`:''}<form method="post" action="/api/replyloop/onboarding"><label>Bedrijfsnaam</label><input name="business_name" value="${esc(a.data.business_name||'')}" required><label>Diensten</label><textarea name="services" placeholder="bv. car detailing, terrasreiniging, loodgieterij">${esc(a.data.services||'')}</textarea><label>Boekings- of contactlink</label><input type="url" name="booking_url" value="${esc(a.data.booking_url||'')}" placeholder="https://..."><label>Antwoordtoon</label><select name="tone"><option${a.data.tone==='kort en direct'?' selected':''}>kort en direct</option><option${a.data.tone==='warm en professioneel'?' selected':''}>warm en professioneel</option><option${a.data.tone==='formeel'?' selected':''}>formeel</option></select><label>Gemiddelde klantwaarde (€)</label><input type="number" name="average_value" min="0" max="1000000" value="${esc(a.data.average_value||250)}"><label>Google-reviewlink (optioneel)</label><input type="url" name="review_url" value="${esc(a.data.review_url||'')}" placeholder="https://..."><div class="actions"><button class="btn" type="submit">Opslaan</button><a class="btn secondary" href="/replyloop/app">Terug</a></div></form></div>`,{authed:true,billingMode});}

  async function handleWebhook(raw,headers){const secret=process.env.LR_STRIPE_WEBHOOK_SECRET||'';if(!verifyStripe(raw,headers['stripe-signature'],secret))throw Object.assign(new Error('Ongeldige Stripe-handtekening'),{status:401});const evt=JSON.parse(raw);const obj=evt.data?.object||{};if(evt.type==='checkout.session.completed'&&obj.payment_status==='paid'){const id=obj.client_reference_id;if(id){const a=await accountById(id);if(a){await saveAccount(id,{...a.data,status:'active',plan:obj.metadata?.plan||a.data.plan,billing:obj.metadata?.billing||a.data.billing,stripe_customer_id:obj.customer||a.data.stripe_customer_id,stripe_subscription_id:obj.subscription||a.data.stripe_subscription_id,activated_at:new Date().toISOString()});await sendMail({to:a.data.email,subject:'ReplyLoop is actief',text:`Dag ${a.data.name},\n\nJe ReplyLoop-account voor ${a.data.business_name} is actief. Rond de eenmalige onboarding af via ${config.baseUrl}/replyloop/onboarding.\n\nReplyLoop`});}}}
    if(['invoice.payment_failed','customer.subscription.deleted','customer.subscription.updated'].includes(evt.type)){const customerId=obj.customer;const notes=await db('list',{table:'customer_notes'});for(const row of notes||[]){const d=noteJson(row);if(d?.lr_type!=='account'||d.stripe_customer_id!==customerId)continue;const a=await accountById(row.customer_id);if(!a)continue;let status=a.data.status;if(evt.type==='invoice.payment_failed')status='past_due';if(evt.type==='customer.subscription.deleted')status='cancelled';if(evt.type==='customer.subscription.updated')status=obj.status==='active'?'active':obj.status;await saveAccount(row.customer_id,{...a.data,status});break;}}
    return {ok:true};}

  async function routes(req,res,h){const {send,json,redirect,sameOrigin}=h;const u=new URL(req.url,config.baseUrl),p=u.pathname;const write=['POST','PUT','PATCH','DELETE'].includes(req.method);
    if(p==='/replyloop.css'&&req.method==='GET'){send(res,200,css,'text/css; charset=utf-8',{'Cache-Control':'public, max-age=3600'});return true;}
    if(!p.startsWith('/replyloop')&&!p.startsWith('/api/replyloop'))return false;
    try{
      if(req.method==='POST'&&p==='/api/replyloop/stripe-webhook'){const raw=await rawBody(req);json(res,200,await handleWebhook(raw,req.headers));return true;}
      if(write&&!sameOrigin(req)&&p!=='/api/replyloop/leads')throw Object.assign(new Error('Ongeldige oorsprong'),{status:403});
      let b={};if(write){const raw=await rawBody(req);b=parseBody(raw,req.headers['content-type']||'');}
      if(req.method==='GET'&&p==='/replyloop'){send(res,200,home(billingMode));return true;}
      if(req.method==='GET'&&p==='/replyloop/pricing'){send(res,200,pricing(billingMode));return true;}
      if(req.method==='GET'&&p==='/replyloop/calculator'){send(res,200,calculatorPage(billingMode));return true;}
      if(req.method==='POST'&&p==='/api/replyloop/calculator'){const leads=Math.max(0,Math.min(100000,Number(b.leads)||0)),value=Math.max(0,Math.min(1000000,Number(b.value)||0)),loss=Math.max(0,Math.min(100,Number(b.loss)||0));const missed=Math.round(leads*loss/100),monthlyLost=Math.round(missed*value),recoverable=Math.round(monthlyLost*.25);send(res,200,calculatorPage(billingMode,{missed,monthlyLost,recoverable}));return true;}
      if(req.method==='GET'&&p==='/replyloop/signup'){send(res,200,signupPage(u.searchParams.get('plan')||'starter',u.searchParams.get('billing')||'monthly',u.searchParams.get('ref')||'',billingMode));return true;}
      if(req.method==='POST'&&p==='/api/replyloop/signup'){const plan=PLANS[b.plan]?b.plan:'starter',billing=['monthly','annual'].includes(b.billing)?b.billing:'monthly';if(!b.business_name||!b.name||!emailOk(b.email)||String(b.password||'').length<10||b.terms!=='yes')throw Object.assign(new Error('Vul alle velden correct in en aanvaard de voorwaarden.'),{status:400});if(await accountByEmail(b.email))throw Object.assign(new Error('Er bestaat al een ReplyLoop-account voor dit e-mailadres.'),{status:409});const customer=await db('customer_create',{name:`[ReplyLoop] ${String(b.business_name).slice(0,90)}`,email:String(b.email).toLowerCase(),phone:'',marketing_consent:false});const pw=passwordRecord(b.password);const data={lr_type:'account',version:1,email:String(b.email).toLowerCase(),name:String(b.name).slice(0,100),business_name:String(b.business_name).slice(0,100),password_salt:pw.salt,password_hash:pw.hash,status:'pending',plan,billing,referrer:String(b.ref||'').slice(0,80),services:'',booking_url:'',tone:'warm en professioneel',average_value:250,review_url:'',created_at:new Date().toISOString()};await saveAccount(customer.id,data);await sendMail({to:data.email,subject:'Je ReplyLoop-account is aangemaakt',text:`Dag ${data.name},\n\nJe account voor ${data.business_name} is aangemaakt. Rond de betaling af om de automatisering te activeren.\n\nReplyLoop`});redirect(res,checkoutUrl(data,customer.id),{'Set-Cookie':setSession(customer.id)});return true;}
      if(req.method==='GET'&&p==='/replyloop/login'){send(res,200,loginPage(billingMode));return true;}
      if(req.method==='POST'&&p==='/api/replyloop/login'){const a=emailOk(b.email)?await accountByEmail(b.email):null;if(!a||!passwordValid(b.password,a.data))throw Object.assign(new Error('Onjuiste e-mail of wachtwoord.'),{status:401});redirect(res,'/replyloop/app',{'Set-Cookie':setSession(a.customer.id)});return true;}
      if(req.method==='POST'&&p==='/api/replyloop/logout'){redirect(res,'/replyloop',{'Set-Cookie':clearSession()});return true;}
      if(req.method==='GET'&&p==='/replyloop/welcome'){const a=await sessionAccount(req);send(res,200,layout('Betaling ontvangen',`<div class="hero"><div><div class="eyebrow">Stripe checkout voltooid</div><h1>Bijna klaar.</h1><p>Stripe verwerkt de bevestiging via een ondertekende webhook. Zodra die binnen is, wordt je abonnement actief.</p><div class="actions"><a class="btn" href="${a?'/replyloop/app':'/replyloop/login'}">Open dashboard</a></div></div><div class="card"><h2>Automatische provisioning</h2><p class="muted">Betaling → accountstatus → onboarding → leadendpoint → follow-upflow.</p></div></div>`,{authed:Boolean(a),billingMode}));return true;}
      if(req.method==='GET'&&p==='/replyloop/app'){const a=await sessionAccount(req);if(!a){redirect(res,'/replyloop/login');return true;}send(res,200,await dashboard(a));return true;}
      if(req.method==='GET'&&p==='/replyloop/onboarding'){const a=await sessionAccount(req);if(!a){redirect(res,'/replyloop/login');return true;}send(res,200,await onboardingPage(a));return true;}
      if(req.method==='POST'&&p==='/api/replyloop/onboarding'){const a=await sessionAccount(req);if(!a)throw Object.assign(new Error('Log opnieuw in'),{status:401});const data={...a.data,business_name:String(b.business_name||a.data.business_name).slice(0,100),services:String(b.services||'').slice(0,1500),booking_url:String(b.booking_url||'').slice(0,500),tone:['kort en direct','warm en professioneel','formeel'].includes(b.tone)?b.tone:'warm en professioneel',average_value:Math.max(0,Math.min(1000000,Number(b.average_value)||0)),review_url:String(b.review_url||'').slice(0,500),onboarded_at:new Date().toISOString()};await saveAccount(a.customer.id,data);redirect(res,'/replyloop/app');return true;}
      if(req.method==='POST'&&p==='/api/replyloop/leads'){const id=publicKeyId(config.cookieSecret,u.searchParams.get('key'));if(!id)throw Object.assign(new Error('Ongeldige formuliercode'),{status:401});const a=await accountById(id);if(!a||a.data.status!=='active')throw Object.assign(new Error('Dit formulier is niet actief'),{status:403});const name=String(b.name||'').trim(),email=String(b.email||'').trim().toLowerCase(),phone=String(b.phone||'').trim();if(!name||(!email&&!phone)||(!emailOk(email)&&email))throw Object.assign(new Error('Naam en geldig contactgegeven zijn verplicht'),{status:400});const all=await store.listLeads(),month=leadsFor(id,all).filter(x=>{const d=new Date(x.created_at),n=new Date();return d.getUTCFullYear()===n.getUTCFullYear()&&d.getUTCMonth()===n.getUTCMonth();});if(month.length>=(PLANS[a.data.plan]?.leadLimit||100))throw Object.assign(new Error('Maandlimiet bereikt'),{status:429});const lead=await store.createLead({name:name.slice(0,120),email:email.slice(0,160),phone:phone.slice(0,60),service:String(b.service||'').slice(0,160),message:String(b.message||'').slice(0,1500),source:`replyloop:${id}`,status:'new',follow_up_at:new Date(Date.now()+24*3600e3).toISOString(),next_action:'ReplyLoop follow-up 1'});if(email){const link=a.data.booking_url?`\n\nJe kunt hier meteen verder: ${a.data.booking_url}`:'';await sendMail({to:email,replyTo:a.data.email,subject:`Aanvraag ontvangen — ${a.data.business_name}`,text:`Dag ${name},\n\nBedankt voor je aanvraag bij ${a.data.business_name}. We hebben je bericht goed ontvangen en komen erop terug.${link}\n\n${a.data.business_name}`});}if((req.headers.accept||'').includes('application/json'))json(res,201,{ok:true,id:lead.id});else send(res,200,layout('Aanvraag ontvangen',`<div class="card"><h1>Bedankt, ${esc(name)}.</h1><p>Je aanvraag voor ${esc(a.data.business_name)} is ontvangen.</p></div>`,{billingMode}));return true;}
      if(req.method==='GET'&&p==='/replyloop/privacy'){send(res,200,legalPage(billingMode,'privacy'));return true;}
      if(req.method==='GET'&&p==='/replyloop/terms'){send(res,200,legalPage(billingMode,'terms'));return true;}
      const niches={'/replyloop/voor-detailers':['detailer','auto-detailers'],'/replyloop/voor-schoonmaakbedrijven':['schoonmaak','schoonmaakbedrijven'],'/replyloop/voor-aannemers':['offerte','aannemers en vakmensen']};if(req.method==='GET'&&niches[p]){send(res,200,nichePage(billingMode,...niches[p]));return true;}
      send(res,404,'Niet gevonden','text/plain; charset=utf-8');return true;
    }catch(e){console.error('ReplyLoop error:',e.message);const status=e.status||500;if(req.method==='GET')send(res,status,layout('Fout',`<div class="card"><h1>Er ging iets mis.</h1><p>${esc(status<500?e.message:'Probeer het later opnieuw.')}</p></div>`,{billingMode}));else if((req.headers.accept||'').includes('application/json'))json(res,status,{error:status<500?e.message:'Interne serverfout'});else send(res,status,layout('Fout',`<div class="card"><h1>Niet gelukt</h1><p>${esc(status<500?e.message:'Interne serverfout')}</p><a class="btn secondary" href="/replyloop">Terug</a></div>`,{billingMode}));return true;}
  }

  async function runAutomation(){const all=await store.listLeads();const due=all.filter(l=>isReplyLoopLead(l)&&l.status==='new'&&l.follow_up_at&&new Date(l.follow_up_at).getTime()<=Date.now()).slice(0,30);let sent=0;for(const lead of due){const id=replyLoopAccountId(lead);const a=await accountById(id).catch(()=>null);if(!a||a.data.status!=='active'||!lead.email)continue;const m=String(lead.next_action||'').match(/follow-up (\d+)/i);const n=m?Number(m[1]):1,max=PLANS[a.data.plan]?.followups||2;if(n>max){await store.updateLead(lead.id,{follow_up_at:null,next_action:'Geen automatische follow-up meer'});continue;}const booking=a.data.booking_url?`\n\nBoeken/contact: ${a.data.booking_url}`:'';const result=await sendMail({to:lead.email,replyTo:a.data.email,subject:`Nog interesse in ${lead.service||a.data.business_name}?`,text:`Dag ${lead.name},\n\nEven een korte opvolging op je aanvraag bij ${a.data.business_name}. Laat gerust weten als we nog kunnen helpen.${booking}\n\n${a.data.business_name}`});if(result.ok){sent++;await store.updateLead(lead.id,{last_outbound_at:new Date().toISOString(),last_contact_at:new Date().toISOString(),follow_up_at:n<max?new Date(Date.now()+(n===1?48:72)*3600e3).toISOString():null,next_action:n<max?`ReplyLoop follow-up ${n+1}`:'Automatische opvolging afgerond'});}}
    return {ok:true,due:due.length,sent};}

  return {routes,runAutomation,_test:{sessionToken,verifySession,passwordRecord,passwordValid,publicKey,publicKeyId,PLANS}};
};
